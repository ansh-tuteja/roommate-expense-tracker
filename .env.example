# ExpenseHub Environment Configuration

# MongoDB Configuration
# Use Atlas in production; replace <db_password> with the user password
# Example Atlas URI:
# mongodb+srv://anshtuteja2978_db_user:<db_password>@expensetracker.gowzj9s.mongodb.net/expensehub?retryWrites=true&w=majority
MONGODB_URI=mongodb://127.0.0.1:27017/expensehub
SESSION_SECRET=your_super_secure_session_secret_here_change_this
SESSION_CRYPTO_SECRET=your_crypto_secret_here_change_this

# Redis Configuration (prefer hosted REDIS_URL in production)
# Example: rediss://default:<password>@<host>:<port
REDIS_URL=
REDIS_HOST=localhost
REDIS_PORT=6379

# Server Configuration
PORT=3000
HTTPS_PORT=3443
NODE_ENV=development

# Security Configuration (if needed)
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Admin token for unlock endpoint (required in production)
ADMIN_UNLOCK_TOKEN=change_me_admin_token
